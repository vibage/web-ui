language: node_js
node_js:
  - "10"

install:
  - npm install

script:
  - npm run build

deploy:
  provider: s3
  access_key_id: "$AWS_KEY"
  secret_access_key: "$AWS_SECRET"
  bucket: "tgt101.com"
  upload-dir: fizzle
  local_dir: dist/front-end
  skip_cleanup: true
  on:
    branch: master
